"# willow-s-action-henk-mods" 

what ive changed:
Modified files:

IRCManager.cs — bonus level Twitch ghost fix
GUI_PreGame.cs — custom Challenge/Bonus ghost select screen
PlayerGraphics.cs — custom skin animation fallback
CameraEffectsManager.cs — environment derender shader swap
DevCommands.cs — performance groups generation, skybox toggle, snow terrain group
PerformanceGroups.cs — particle system toggle support, removed re-init skip guard
ActionHenk.cs — MOTD credit string
ReplayRecorder.cs — skin remap hook in SaveReplayToString
EventDelegate.cs — compile fix (fieldName rename)
MegaShapeSXL.cs — compile fix (int num = 0)

New file:

ReplaySkinRemap.cs — skin remapping logic for doomguy's stuff, may aswell include it here
